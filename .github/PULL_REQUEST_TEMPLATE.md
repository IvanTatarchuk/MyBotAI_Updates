## What

## Why

## Checklist

- [ ] `pytest -q` passes
- [ ] If a rule was added/changed: fixtures added for both a case that should
      trigger it and one that deliberately shouldn't
- [ ] If CLI behavior changed: `README.md` example output updated to match
